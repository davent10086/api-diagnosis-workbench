import {describe,it,expect} from "vitest";import {runRules} from "./rules";
describe("P0 规则",()=>{it("识别 Bedrock 工具不兼容",()=>{const r=runRules({route:"bedrock",statusCode:400,clientRequest:{tools:[{type:"web_search_20250305"}]},upstreamResponse:{type:"ValidationException"}});expect(r.some(x=>x.ruleId==="bedrock-tool-compatibility")).toBe(true)});it("5xx 缺上游响应时不归因",()=>{const r=runRules({statusCode:502});expect(r.find(x=>x.ruleId==="http-5xx")?.needsMoreEvidence).toBe(true)});it("检测 SSE stop 缺少 start",()=>{expect(runRules({sse:["event: content_block_stop index: 1"]}).some(x=>x.ruleId==="anthropic-sse-lifecycle")).toBe(true)});});
