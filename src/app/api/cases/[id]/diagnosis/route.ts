import { NextResponse } from "next/server";
import { z } from "zod";
import { DiagnosisConflictError, DiagnosisNotFoundError } from "@/lib/diagnosis";
import { enqueueDiagnosis } from "@/lib/diagnosis-queue";
const requestSchema = z.object({ reasoningEffort: z.enum(["low", "high", "max"]).default("high") });
export async function POST(request: Request, { params }: { params: Promise<{ id: string }> }) { const { id } = await params; if (!z.string().uuid().safeParse(id).success) return NextResponse.json({ error: "案件 ID 无效。" }, { status: 400 }); const parsed = requestSchema.safeParse(await request.json().catch(() => ({}))); if (!parsed.success) return NextResponse.json({ error: "推理强度只能为低、高或最高。" }, { status: 400 }); try { const jobId = await enqueueDiagnosis(id, parsed.data.reasoningEffort); return NextResponse.json({ jobId, status: "queued" }, { status: 202 }); } catch (error) { if (error instanceof DiagnosisNotFoundError) return NextResponse.json({ error: error.message }, { status: 404 }); if (error instanceof DiagnosisConflictError) return NextResponse.json({ error: error.message }, { status: 409 }); return NextResponse.json({ error: error instanceof Error ? error.message : "无法加入 AI 诊断队列。" }, { status: 503 }); } }
