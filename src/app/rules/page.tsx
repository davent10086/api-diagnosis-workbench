import {Shell,Top} from "@/components/workbench";const rules=["HTTP 429 限流与重试证据","HTTP 5xx 上游响应边界","取消、超时与提前断流","Anthropic SSE 生命周期","协议字段 Diff","Bedrock 工具能力兼容性","缓存证据充分性","工具调用生命周期"];export default function Rules(){return <Shell><Top title="规则中心"/><div className="p-6"><h1 className="text-xl font-bold">规则中心</h1><p className="mt-1 text-sm text-muted">确定性规则独立于模型，规则证据不会被诊断模型覆盖。</p><div className="panel mt-5 divide-y">{rules.map((x,i)=><div className="flex items-center justify-between p-4" key={x}><div><b>{x}</b><p className="mt-1 text-sm text-muted">P0 · 可测试 · 最近命中 {i===5?1:0} 次</p></div><span className="badge bg-emerald-50 text-emerald-700">已启用</span></div>)}</div></div></Shell>}
