import {Shell,Top} from "@/components/workbench";export default function Settings(){const configured=Boolean(process.env.OPENAI_API_KEY&&process.env.OPENAI_BASE_URL);return <Shell><Top title="设置"/><div className="p-6"><h1 className="text-xl font-bold">本地设置</h1><div className="panel mt-5 divide-y"><div className="p-4"><b>模型服务</b><p className="mt-2 text-sm text-muted">{configured?"已配置 OpenAI-compatible 服务":"未配置：使用手动编辑、种子数据与规则引擎演示"}</p><p className="mt-1 text-sm">视觉模型：{process.env.VISION_MODEL??"未配置"} · 分析模型：{process.env.ANALYSIS_MODEL??"未配置"}</p></div><div className="p-4"><b>数据存储</b><p className="mt-2 text-sm text-muted">PostgreSQL：{process.env.DATABASE_URL?"已配置":"未配置"}；原始文件：项目 storage/ 目录</p></div><div className="p-4 text-sm text-muted">密钥不会在此页面显示，也不会保存到案件、证据或日志中。</div></div></div></Shell>}
